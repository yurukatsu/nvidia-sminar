[project]
name = "nvidia-seminar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mistralai>=2.0.0",
    "typer>=0.24.1",
]
